# Core scientific stack
numpy>=1.24
scipy>=1.11
pandas>=2.0
matplotlib>=3.7

# Deep learning (CPU build is sufficient for the fPINN training in fpinn.py)
torch>=2.0

# Optional: LaTeX rendering of mathematical text in matplotlib figures
# (matplotlib falls back to mathtext if a TeX installation is not detected)
